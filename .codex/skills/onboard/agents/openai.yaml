interface:
  display_name: "Onboard"
  short_description: "Generates a contextual onboarding document for a new..."
  default_prompt: "Use $onboard to generates a contextual onboarding document for a new contributor or agent joining the project."
policy:
  allow_implicit_invocation: false
