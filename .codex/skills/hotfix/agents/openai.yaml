interface:
  display_name: "Hotfix"
  short_description: "Emergency fix workflow that bypasses normal sprint..."
  default_prompt: "Use $hotfix to emergency fix workflow that bypasses normal sprint processes with a full audit trail."
policy:
  allow_implicit_invocation: false
