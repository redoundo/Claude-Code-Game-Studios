interface:
  display_name: "Brainstorm"
  short_description: "Guided game concept ideation — from zero idea to a..."
  default_prompt: "Use $brainstorm to guided game concept ideation — from zero idea to a structured game concept document."
policy:
  allow_implicit_invocation: false
