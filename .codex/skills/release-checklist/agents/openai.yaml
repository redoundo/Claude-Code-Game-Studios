interface:
  display_name: "Release Checklist"
  short_description: "Generates a comprehensive pre-release validation checklist..."
  default_prompt: "Use $release-checklist to generates a comprehensive pre-release validation checklist covering build verification, certification requirements, store metadata, and launch readiness."
policy:
  allow_implicit_invocation: false
