interface:
  display_name: "Asset Audit"
  short_description: "Audits game assets for compliance with naming conventions,..."
  default_prompt: "Use $asset-audit to audits game assets for compliance with naming conventions, file size budgets, format standards, and pipeline requirements."
policy:
  allow_implicit_invocation: false
