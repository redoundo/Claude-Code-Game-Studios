interface:
  display_name: "Changelog"
  short_description: "Auto-generates a changelog from git commits, sprint data,..."
  default_prompt: "Use $changelog to auto-generates a changelog from git commits, sprint data, and design documents."
policy:
  allow_implicit_invocation: false
