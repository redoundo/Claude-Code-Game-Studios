interface:
  display_name: "Estimate"
  short_description: "Estimates task effort by analyzing complexity,..."
  default_prompt: "Use $estimate to estimates task effort by analyzing complexity, dependencies, historical velocity, and risk factors."
policy:
  allow_implicit_invocation: false
