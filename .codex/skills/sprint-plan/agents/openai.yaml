interface:
  display_name: "Sprint Plan"
  short_description: "Generates a new sprint plan or updates an existing one..."
  default_prompt: "Use $sprint-plan to generates a new sprint plan or updates an existing one based on the current milestone, completed work, and available capacity."
policy:
  allow_implicit_invocation: false
