interface:
  display_name: "Code Review"
  short_description: "Performs an architectural and quality code review on a..."
  default_prompt: "Use $code-review to performs an architectural and quality code review on a specified file or set of files."
policy:
  allow_implicit_invocation: false
